---
source: lib/typescript-reference/src/main.rs
expression: output
---
### @example/test

### Default export

This is the docs.

```{.typescript .no-copy}
function add(a: number, b: number): number;
```

#### Exports

##### MAGIC_NUMBER

I'm a magic number.

```{.typescript .no-copy}
const MAGIC_NUMBER: number
```

##### PiResult

The interface.

```{.typescript .no-copy}
interface PiResult {
  pi: number;
}
```

##### bar

there

```{.typescript .no-copy}
const foo: number, bar: number
```

##### foo

hello

```{.typescript .no-copy}
const foo: number, bar: number
```

##### getPi

Gets the number.

```{.typescript .no-copy}
function getPi(): PiResult;
```

##### multiply

It's good.

```{.typescript .no-copy}
function multiply(a: number, b: number): number;
```

#### Reexports

```{.typescript .no-copy}
export { other } from "@example/test/foo";
```
### @example/test/foo

#### Exports

##### Other

I'm a doc comment.

```{.typescript .no-copy}
namespace Other {
  export function inner(): Internal;
  function unexported(): Internal;
  const INNER: number;
}
```

##### other

Very cool.

```{.typescript .no-copy}
const other: number
```
